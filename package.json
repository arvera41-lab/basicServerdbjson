{
  "name": "servidor_json_01",
  "version": "0.0.1",
  "description": "Servidor de json con un archvio json como DB",
  "license": "ISC",
  "author": "Alda  Code",
  "type": "module",
  "main": "src/Middleware/main.js",
  "scripts": {
    "start": "concurrently \"json-server --watch db.json --port 8080\" \"node src/Middleware/main.js\"",
    "test": "echo Error: no test specified",
    "dev": "nodemon test.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.11"
  }
}
